spring:
  application:
    name: lovable-clone
    description: this is application

  datasource:
    url: jdbc:postgresql://localhost:9010/pgvector-test?options=-c%20timezone=Asia/Kolkata
    username: user
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

  ai:
    openai:
      api-key: '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'
      base-url: https://openrouter.ai/api
      chat:
        options:
          model: openai/gpt-oss-120b:free
          temperature: 0.0
          max-tokens: 400

jwt:
  secret-key: jhsfd45646jhhsdu8554gygfvdasg46544454

stripe:
  api:
    secret: \${STRIPE_API_SECRET:sk_test_placeholder}
  webhook:
    secret: \${STRIPE_WEBHOOK_SECRET:whsec_placeholder}

client.url: http://localhost:8080


logging:
  level:
    org.springframework.ai:
      chat.client: DEBUG

minio:
  url: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin123
  project-bucket: projects
